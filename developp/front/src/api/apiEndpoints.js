// src/api/apiEndpoints.js

// Endpoints pour les cartes
export const CARDS_ENDPOINT = "/cards";

// Endpoints pour les tirages
export const DRAWINGS_ENDPOINT = "/drawings";

// Endpoints pour les commentaires
export const REVIEWS_ENDPOINT = "/reviews";

// Endpoints pour les rôles
export const ROLES_ENDPOINT = "/roles";

// Endpoints pour les thèmes
export const THEMES_ENDPOINT = "/themes";

// Endpoints pour les utilisateurs
export const USERS_ENDPOINT = "/users";

// Endpoints pour l'authentification
export const AUTH_ENDPOINT = {
  LOGIN: "/auth/login",
  SIGNUP: "/users",
  FORGOT_PASSWORD: "/auth/forgot-password",
  RESET_PASSWORD: "/auth/reset-password",
};

// Endpoints pour les téléchargements
export const UPLOAD_ENDPOINT = "/upload";
